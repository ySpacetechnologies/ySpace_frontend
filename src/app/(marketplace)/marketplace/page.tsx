import { PagePlaceholder } from "@/components"

export const metadata = { title: "Marketplace" }

export default function MarketplacePage() {
  return <PagePlaceholder eyebrow="Marketplace" title="Shop the ySpace marketplace" description="Products from vetted vendors, delivered by the ySpace network." />
}
