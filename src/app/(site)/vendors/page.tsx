import { PagePlaceholder } from "@/components"

export const metadata = { title: "Vendors" }

export default function VendorsPage() {
  return <PagePlaceholder eyebrow="Vendors" title="Partner with ySpace" description="Sell and deliver through the ySpace marketplace." />
}
