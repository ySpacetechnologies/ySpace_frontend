import { PagePlaceholder } from "@/components"

export const metadata = { title: "Company" }

export default function CompanyPage() {
  return <PagePlaceholder eyebrow="Company" title="The team behind the network" description="Who we are and why we are building ySpace." />
}
